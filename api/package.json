{
  "name": "melior-agent-api",
  "version": "1.0.0",
  "description": "LiveKit token generation API for Melior Agent voice chat",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "livekit-server-sdk": "^2.14.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "jest": "^30.2.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  }
}
